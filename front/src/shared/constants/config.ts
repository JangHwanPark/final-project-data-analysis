// 한 페이지당 아이템 수
const PAGE_SIZE = 20;

// 파일 업로드 제한 (MB)
const MAX_FILE_SIZE_MB = 10;

// 토스트 메시지 떠있는 시간 (ms)
const TOAST_DURATION = 3000;

// 날짜 표시 형식
const DATE_FORMAT = 'YYYY-MM-DD';

export const CONFIG = {
  PAGE_SIZE,
  MAX_FILE_SIZE_MB,
  TOAST_DURATION,
  DATE_FORMAT
};